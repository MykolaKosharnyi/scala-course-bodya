package dev.study.lessons

object Lesson9 {
  // monade requires -> has map (flatMap), apply Unit

  // learn testing

  def sum(first: Int, second: Int): Int = first + second
  def devide(first: Int, second: Int): Int = first / second
}
